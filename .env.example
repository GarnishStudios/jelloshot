# Environment Variables Template
# Copy this file to .env and fill in your actual values
# .env is gitignored and should contain your real secrets

# Database Configuration
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres
DATABASE_HOSTNAME=postgres
DATABASE_PORT=5432
DATABASE_NAME=callsheet

# Application Security
SECRET_KEY=your-secret-key-here-change-in-production
SESSION_SECRET=your-session-secret-here-change-in-production

# OAuth Configuration (Required for Login)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# For production, use strong passwords and real hostnames:
# DATABASE_USER=callsheet_prod_user
# DATABASE_PASSWORD=<generate with: openssl rand -base64 32>
# DATABASE_HOSTNAME=your-rds-instance.region.rds.amazonaws.com
# SECRET_KEY=<generate with: openssl rand -hex 32>
